---

- name: Install common packages
  package:
    name: "{{ item }}"
    state: present
  with_items:
  - htop
  - ufw
  - openssh-server
  - unzip
  - python-pip

- name: Besure ssh server is running
  service: 
    name: ssh 
    state: running

- name: Open ssh port
  ufw:
    rule: allow
    port: 22
